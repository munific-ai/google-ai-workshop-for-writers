# 環境構築手順書

**対象読者**：ワークショップの準備担当者・講師・管理者
**参加者向けの資料は [`HANDSON.md`](HANDSON.md) です。この手順書は参加者に配布しないでください**（仕込んだ誤りの正解が含まれます）。

---

## 0. この手順書の使い方

接続するのは **Google Drive / Google Chat / Gmail の 3 つだけ**です。
すべて Google Workspace 内なので、外部 SaaS のコネクタ設定が不要になり、準備は大幅に軽くなりました。

| 日      | やること                                                             | 所要 |
| ------- | -------------------------------------------------------------------- | ---- |
| **D-4** | §2 前提確認（**スマート機能の有効化を含む**）→ §3 Drive              | 2h   |
| **D-3** | §4 Google Chat → §5 Gmail                                            | 1.5h |
| **D-2** | §6 コネクタ設定と同期確認（**3 コネクタとも OAuth 設定が必要**）     | 2h   |
| **D-1** | §7 受け入れテスト（**最重要**）→ §8 参加者アカウント → §9 リハーサル | 3h   |

> ⚠️ **§7 の受け入れテストは省略しないでください。**
> 仕込んだ「発見」が実際に返ってくるかを確認する工程です。ここを飛ばすと、当日エージェントが期待した指摘を返さず、山場が成立しません。

---

## 1. 全体像

```
                 ┌──────────────────────┐
                 │  Gemini Enterprise   │
                 └──────────┬───────────┘
          ┌─────────────────┼─────────────────┐
          │                 │                 │
    Google Drive      Google Chat           Gmail
   ┌──────┴──────┐   ┌──────┴──────┐    広報とのやり取り
 マイドライブ  共有ドライブ  特集/雑談/★校閲・訂正
 取材メモ      用字用語ガイド
 原稿ドラフト   会社概要・業績資料
              過去記事・広報資料
              取材管理台帳・庶務
```

| Part   | 使うもの                                                     | 事前準備        |
| ------ | ------------------------------------------------------------ | --------------- |
| Part 1 | Gemini（公開版）+ 実在ブログ                                 | §5.3 の保険のみ |
| Part 2 | Workspace with Gemini（Sheets）+ マイドライブの取材メモ 3 本 | §3.1            |
| Part 3 | Gemini Enterprise + Drive / Chat / Gmail                     | §3〜§6 すべて   |

---

## 2. 前提条件の確認（D-4）

### 2.1 ライセンスと権限

| 項目                         | 必要なもの                                                                                      |
| ---------------------------- | ----------------------------------------------------------------------------------------------- |
| Gemini Enterprise            | 管理者権限のあるテナント                                                                        |
| **Agent Designer**           | **管理者による有効化が必要**。無効だと Part 3 が成立しません                                    |
| Google Workspace             | Gemini が有効な組織アカウント。**Sheets のサイドパネルで Gemini が使えること**（Part 2 の主役） |
| 共有ドライブ                 | 作成権限                                                                                        |
| **Workspace 管理コンソール** | **「他の Google プロダクトのスマート機能」を有効化できる権限**（→ §6.1）                        |
| **ID プロバイダー**          | Gemini Enterprise 側で設定済みであること。**権限デモの前提でもあります**（→ §6.1）              |

> ⚠️ **「スマート機能」の有効化は D-4 のうちに済ませてください。**
> これが無効だと、D-2 のコネクタ設定で**どのデータストアも作成できません**。
> 管理コンソール側の設定なので、権限を持つ人への依頼が必要な場合は特に早めに。

### 2.2 ⚠️ 20 名運用の前提（重要）

**参加者 20 名がそれぞれ別の Google アカウントで、同一テナントにログインします。**
この前提から、次の 5 点を必ず守ってください。

#### (1) 参加者が書き込むものは 2 つだけ

| 参加者が作るもの          | どこにできるか     | 衝突                  |
| ------------------------- | ------------------ | --------------------- |
| Part 2 のスプレッドシート | 各自のマイドライブ | なし                  |
| Part 3 のエージェント     | 共有環境           | **あり → (2) で対処** |

**それ以外のデータはすべて閲覧専用で配布します。** 参加者ごとのファイル複製は不要です。
（原稿ドラフトも、Part 3 では本文をコピーして貼るだけなので編集は発生しません）

#### (2) 参加者番号を配る

エージェント名の衝突を防ぐため、**参加者番号 01〜20 を席のカードで配布**してください。

- ✅ `原稿レビュー_07`
- ❌ `原稿レビュー_佐藤` … 同姓の参加者がいると衝突し、入力ゆれも起きます

講師が一覧で進捗を把握するうえでも、番号のほうが確実です。

#### (3) 権限は Google グループで一括付与する

20 名を個別に追加すると、漏れが必ず出ます。

```
techscope-workshop@（あなたのドメイン）
```

というグループを作り、参加者 20 名を入れてください。以下はすべてこのグループに付与します。

| 対象                            | 付与する権限                 |
| ------------------------------- | ---------------------------- |
| 共有ドライブ `TECHSCOPE 編集部` | **閲覧者**                   |
| Chat スペース 3 つ              | メンバー                     |
| `編集長限定` フォルダ           | **付与しない**（権限デモ用） |

#### (4) Gmail は全員宛に送る

参加者 20 名の受信箱に同じメールが必要です。**§5 の手順で 3 通を全員宛に送信**してください。

> ⚠️ 「佐倉 → 若林」の送信メールは、参加者の**送信済み**に置く必要があり 20 アカウントでは用意できません。
> そのため **3 通すべてを広報役アカウントから受信させる構成**に変更済みです。
> 佐倉の依頼内容は 1 通目に引用として含まれており、受信箱だけで文脈が完結します。

#### (5) 共有ドライブは閲覧者権限にする

編集者権限を与えると、誰かが誤ってファイルを書き換えたときに**全員のハンズオンが壊れます**。
必ず**閲覧者**にしてください。

### 2.3 架空データと実在企業の扱い（必読）

本シナリオでは **Google Cloud が取材相手として登場します。実在企業です。**
詳細は [`docs/story-bible.md`](docs/story-bible.md) §0 を必ず読んでください。

| 対象                                                            | 扱い                                                                                                        |
| --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| SOLVRA株式会社、静和精機、京浜運輸、東雲デジタル銀行、TECHSCOPE | **すべて架空**。仕込みの誤りはすべてここに寄せてある                                                        |
| **Google Cloud**                                                | **実在**。VP の氏名は設定せず、発言・価格・実績は一切データに登場させない。取材は物語上「明日」＝**未実施** |

**冒頭スライドで次の 2 点を必ず明示してください。**

1. SOLVRA 社などの企業・人物・数値はすべて架空であること
2. Google Cloud は実在するが、**取材は未実施の設定**であり、発言は一切登場しないこと

---

## 3. Google Drive（D-4）

### 3.0 ファイル形式の変換

`data/` 配下の Markdown / CSV を Google 形式に変換します。

> ⚠️ 各ファイル冒頭の HTML コメント（`<!-- ... -->`）は**準備担当者向けの注記**です。
> **変換前に必ず削除してください。** 参加者に見せるものではありません。

| 元     | 変換先                  |
| ------ | ----------------------- |
| `.md`  | Google ドキュメント     |
| `.csv` | Google スプレッドシート |

手動の場合：Drive にアップロード → 右クリック →「アプリで開く」→「Google ドキュメント／スプレッドシート」。

### 3.1 編集部共有ドライブ（すべてここに置きます）

`TECHSCOPE 編集部` という共有ドライブを作成し、以下を配置します。
**参加者グループには閲覧者権限のみ**を与えてください（→ §2.2）。

| フォルダ            | 元データ                                                              | 備考                                          |
| ------------------- | --------------------------------------------------------------------- | --------------------------------------------- |
| `取材メモ/`         | `data/drive/my-drive/取材メモ/`                                       | **★Part 2 で `@` 指定する 3 本を含む**        |
| `特集2026年8月/`    | `data/drive/my-drive/特集2026年8月/`                                  | 原稿ドラフト（誤り 10 か所＋方針違反 3 か所） |
| `編集ハンドブック/` | `data/drive/shared-drive/編集ハンドブック/`                           | 用字用語ガイド＋**特集の編集方針**            |
| `記事アーカイブ/`   | `data/drive/shared-drive/記事アーカイブ/`                             |                                               |
| `取材先資料/`       | `data/drive/shared-drive/取材先資料/`                                 |                                               |
| `広報資料/`         | `data/drive/shared-drive/広報資料/`                                   |                                               |
| `取材管理台帳/`     | `data/drive/shared-drive/取材管理台帳/`（**CSV → スプレッドシート**） |                                               |
| `庶務/`             | `data/drive/shared-drive/庶務/`（**CSV → スプレッドシート**）         |                                               |

> 💡 **リポジトリ上は `my-drive/` と `shared-drive/` に分かれていますが、実際の配置先は共有ドライブ 1 つです。**
> 20 名がそれぞれ別アカウントで参加するため、参加者ごとのマイドライブ配置は行いません。
> ディレクトリ名は「もともと記者個人の手元にあった資料」という物語上の区別を残しているだけです。

**Part 2 で参加者が `@` 指定するのは、`取材メモ/` の次の 3 本だけです。**

- `interview-notes-seiwa`
- `interview-notes-keihin`
- `interview-notes-shinonome`

### 3.2 権限デモ用（**強く推奨**）

`編集長限定` というフォルダを作り、次の 2 ファイルを配置します。
**参加者グループにはアクセス権を与えないでください。**

| ファイル                                                          | 内容                                             | カナリア語                      |
| ----------------------------------------------------------------- | ------------------------------------------------ | ------------------------------- |
| `data/drive/shared-drive/編集長限定/2026年下期-特集企画リスト.md` | 未発表の調査報道企画。**情報源保護の記述を含む** | `クロシオ` / `PROJECT-KUROSHIO` |
| `data/drive/shared-drive/編集長限定/編集部-要員計画.md`           | 本人未通知の人事検討                             | `要員計画` / `増員枠`           |

> 💡 **なぜこの題材か**
> 「未発表の調査報道と、その情報源」は、記者にとって
> **絶対に漏れてはいけないものの代表**です。
> 「AI に社内情報を全部読ませて大丈夫なのか」という懸念に、最も直接的に答えられます。

**カナリア語**は、この 2 ファイル以外のどこにも登場しません。
受け入れテスト（§7.6）で、参加者アカウントから検索して **0 件**であることを確認します。

> ⚠️ ファイル本文に「これは権限デモ用です」といった注記は**入れていません**。
> 万一参加者に見えてしまった場合でも、文書として自然に読める必要があるためです。
> 準備担当者向けの説明は、冒頭の HTML コメントにあります（投入前に削除してください）。

---

## 4. Google Chat（D-3）

### 4.1 スペースの作成

| スペース名       | 元データ                                  | 役割                               |
| ---------------- | ----------------------------------------- | ---------------------------------- |
| `特集-data-2026` | `data/chat/spaces/tokushu-data-2026.json` | 特集の進行                         |
| `編集部-general` | `data/chat/spaces/henshubu-general.json`  | 雑談                               |
| **`校閲・訂正`** | `data/chat/spaces/koetsu-teisei.json`     | **★クライマックス。COR-2023-0147** |

**参加者アカウントを全スペースのメンバーに追加してください。** メンバーでないと検索対象になりません。

### 4.2 OAuth の設定（初回のみ）

`data/chat/scripts/post_messages.py` は **ユーザー認証（OAuth）** で動きます。
投稿は、**認証したアカウント本人の発言**として記録されます。

#### 必要なもの一覧

| 項目                           | 値                                                                      |
| ------------------------------ | ----------------------------------------------------------------------- |
| API                            | **Google Chat API**（**有効化**と**構成**の両方が必要）                 |
| OAuth スコープ                 | `https://www.googleapis.com/auth/chat.messages.create`                  |
| OAuth クライアントの種類       | **デスクトップ アプリ**                                                 |
| OAuth 同意画面のユーザーの種類 | **内部（Internal）**                                                    |
| 認証するアカウント             | **投稿先スペースのメンバーであること**                                  |
| 認証情報                       | `credentials.json`（ダウンロード）／ `token.json`（初回認証で自動生成） |

#### 手順

**1. Google Cloud プロジェクトで Chat API を有効化する**

**2. Chat API を「構成」する** ← ⚠️ **ここが最も詰まります**

読み取りだけなら構成は不要ですが、**メッセージの作成には構成が必須**です。
飛ばすと、実行時に次のエラーになります。

```
HttpError 404 ... "Google Chat app not found. To create a Chat app,
you must turn on the Chat API and configure the app in the Google Cloud console."
```

**API を「有効化」しただけでは足りません。** 次の画面を開いてください。

```
https://console.cloud.google.com/apis/api/chat.googleapis.com/hangouts-chat
```

「**構成 / Configuration**」タブで、次を設定して保存します。

| セクション           | 項目             | 要件                                | 例                     |
| -------------------- | ---------------- | ----------------------------------- | ---------------------- |
| アプリ情報           | アプリ名         | **必須**。25 文字以内の英数字       | `TECHSCOPE Loader`     |
| アプリ情報           | アバター URL     | **必須**。HTTPS の正方形 PNG / JPEG | 下記からコピー         |
| アプリ情報           | 説明             | **必須**。40 文字以内の英数字       | `Workshop data loader` |
| インタラクティブ機能 | 有効にするトグル | **オフにする**                      | —                      |

**アバター URL はこれをそのまま貼ってください**（動作確認済み）

```
https://developers.google.com/chat/images/quickstart-app-avatar.png
```

Google 公式のクイックスタートで使われている汎用アバターです（32×32 の正方形 PNG）。
「256×256 以上」は**推奨**であって必須ではないため、これで保存できます。

大きめの画像がよければ、こちらでも通ります（256×256 の無地）。

```
https://placehold.co/256x256.png
```

> ⚠️ 自社ロゴや Google のプロダクトロゴは使わないでください。
> このアプリは**データ投入のための一時的なもの**です。

> 💡 **インタラクティブ機能は必ずオフにしてください。**
> 今回はボット（Chat アプリ）を作るわけではなく、**ユーザーとして投稿するだけ**です。
> オンのままだと接続設定（HTTP エンドポイント URL や Pub/Sub）の入力を求められ、
> 用意する必要のないものを作るはめになります。

保存後、反映まで数分かかることがあります。

**3. OAuth 同意画面を確認する**

- ユーザーの種類：**内部（Internal）**
  > ⚠️ **外部（External）にしないでください。**
  > `chat.messages.create` は機微なスコープにあたり、外部にすると **Google の審査**が必要になります。
  > 準備が数日単位で止まります。

> 💡 **スコープの事前登録は不要です。**
> 同意画面が「内部」であれば、スクリプトが実行時にスコープを要求し、
> ブラウザの承認画面でそのまま許可できます。
> （画面を確認したい場合、現在のコンソールでは
> **Google Auth Platform → データアクセス** にあります。
> 以前の「OAuth 同意画面 → スコープ」からは移動しています。）

**4. OAuth クライアント ID を作成する**

- アプリケーションの種類：**デスクトップ アプリ**
  > ⚠️ 「ウェブ アプリケーション」で作ると `redirect_uri_mismatch` になります。
- JSON をダウンロードし、**`credentials.json`** という名前で `data/` 直下に置く

**5. 初回実行で認証する**

`--dry-run` を外して実行するとブラウザが開きます。
**投稿先スペースのメンバーであるアカウント**でログインし、承認してください。

成功すると `token.json` が生成され、2 回目以降は認証不要になります。

> ⚠️ **このクライアントは、このスクリプト専用です。**
> Gemini Enterprise のコネクタ用には、**別途もう 1 つ**（ウェブ アプリケーション種別、
> リダイレクト URI 付き）を §6.2 で作ります。種類もスコープも異なります。

> ⚠️ `credentials.json` と `token.json` は**絶対にコミットしないでください。**
> `token.json` には**リフレッシュトークン**が含まれます。どちらも `.gitignore` に登録済みです。

#### スペース ID の調べ方

Google Chat で対象スペースを開き、URL を確認します。

```
https://chat.google.com/app/chat/xxxxxxxxxxx
                                 ~~~~~~~~~~~ これがスペース ID
```

スクリプトには **`spaces/` を付けて**渡します（例：`--space spaces/xxxxxxxxxxx`）。

#### つまずいたら

| エラー                                  | 原因                                             | 対処                                          |
| --------------------------------------- | ------------------------------------------------ | --------------------------------------------- |
| `403 PERMISSION_DENIED`                 | 認証したアカウントがスペースの**メンバーでない** | そのアカウントをスペースに参加させる          |
| `403` で構成に言及される                | Chat API の「構成」タブが未入力                  | 手順 2 を実施する                             |
| `Access blocked … verification process` | 同意画面が「外部」                               | **「内部」**に変更する                        |
| `redirect_uri_mismatch`                 | OAuth クライアントの種類が違う                   | **デスクトップ アプリ**で作り直す             |
| `400 INVALID_ARGUMENT`                  | `--space` の形式                                 | `spaces/XXXX` の形にする                      |
| `429` / `RESOURCE_EXHAUSTED`            | 投稿間隔が短い                                   | `--delay 2` などに増やす                      |
| `invalid_grant`                         | `token.json` が古い／スコープを変えた            | `token.json` を削除して再認証                 |
| 依存パッケージが不足                    | 未インストール                                   | `uv sync`（`--dry-run` は依存なしで動きます） |

---

### 4.3 メッセージの投入

`data/chat/scripts/post_messages.py` を使います。

```bash
cd data && source .venv/bin/activate && uv sync

# まず内容を確認（依存なしでも動きます）
python chat/scripts/post_messages.py \
  --space spaces/xxxxxxxxxxx --json chat/spaces/henshubu-general.json \
  --dry-run --prefix-author
python chat/scripts/post_messages.py \
  --space spaces/yyyyyyyyyyy --json chat/spaces/koetsu-teisei.json \
  --dry-run --prefix-author
python chat/scripts/post_messages.py \
  --space spaces/zzzzzzzzzzz --json chat/spaces/tokushu-data-2026.json \
  --dry-run --prefix-author

# 実際に投入
python chat/scripts/post_messages.py \
  --space spaces/xxxxxxxxxxx --json chat/spaces/henshubu-general.json \
  --prefix-author
python chat/scripts/post_messages.py \
  --space spaces/yyyyyyyyyyy --json chat/spaces/koetsu-teisei.json \
  --prefix-author
python chat/scripts/post_messages.py \
  --space spaces/zzzzzzzzzzz --json chat/spaces/tokushu-data-2026.json \
  --prefix-author
```

3 スペース分、`--space` と `--json` を変えて 3 回実行します。

> **`--prefix-author` について**｜4 名分のアカウントを用意できない場合、本文の先頭に `[沢田 亮]` のように発言者名を付けて 1 アカウントから投稿します。横断検索は本文で拾うため、これで十分機能します。

### 4.4 投稿日時について

**Chat API では過去日時を指定できません。** JSON の `timestamp` は再現されず、投稿は実行時刻になります。
横断検索は本文の内容で拾うため実害はありませんが、**当日「◯月◯日のチャットで」という言い方は避けてください。**

### 4.5 ⚠️ 最重要：`校閲・訂正` スペース

**このスペースがワークショップの山場です。** 必ず投入し、参加者が閲覧できることを確認してください。

訂正記録 COR-2023-0147 は**ここにしか存在しません**。Google ドキュメントのサイドパネルから Chat を `@` で参照することはできないため、**Part 2 のやり方では構造的に到達できず、Part 3 でのみ発見される**という設計です。

---

## 5. Gmail（D-3）

### 5.1 メールの投入

`data/gmail/solvra-koho-thread.md` の **3 通**を投入します。

**広報役のダミーアカウント（若林 千尋）を 1 つ作り、そこから参加者全員宛に送信**してください。
件名を引き継いで 1 スレッドにまとめます。

| 手順 |                                                        |
| ---- | ------------------------------------------------------ |
| 1    | 広報役アカウントを 1 つ作成する                        |
| 2    | 宛先に参加者グループ `techscope-workshop@` を指定する  |
| 3    | 3 通を順に送信する（2・3 通目は 1 通目への返信として） |

> ⚠️ **参加者を BCC にしないでください。** BCC だと受信者側で宛先が見えず、
> 「自分（佐倉）宛のメール」という感覚が薄れます。To かグループ宛にしてください。

> 💡 **「佐倉 → 若林」の送信メールは作りません。**
> 参加者の【送信済み】に置く必要があり、20 アカウント分は用意できないためです。
> 佐倉の依頼内容は 1 通目に**引用**として含めてあり、受信箱だけで文脈が完結します。

### 5.2 ⚠️ Google Cloud 広報からのメールは作りません

実在企業の通信を捏造することになるためです。
8/5 の取材アポの記録は、**共有ドライブの取材管理台帳と Chat**（＝佐倉自身のデータ）にのみ置いています。

### 5.3 Part 1 の保険（重要）

Part 1 では [TabFM のブログ記事](https://research.google/blog/introducing-tabfm-a-zero-shot-foundation-model-for-tabular-data/) を Gemini に読ませます。

---

## 6. コネクタ設定と同期確認（D-2）

Gemini Enterprise で **Google Drive / Google Chat / Gmail** の 3 つのデータストアを作ります。

> 📖 一次情報：
> [Google Drive](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gdrive/set-up-data-store) /
> [Google Chat](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gchat/set-up-data-store) /
> [Gmail](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gmail/set-up-data-store)

### 6.1 3 つ共通の前提（これを満たさないと作成できません）

| 前提                | 内容                                                                                          |
| ------------------- | --------------------------------------------------------------------------------------------- |
| **アカウント**      | Google Cloud コンソールに、**Workspace インスタンスと同じアカウント**でサインインしていること |
| **アカウント種別**  | **個人の `@gmail.com` は不可。** カスタマー ID を持つ組織アカウントが必要です                 |
| **スマート機能**    | Workspace 管理コンソールで「**他の Google プロダクトのスマート機能**」を**有効化**            |
| **ID プロバイダー** | アクセス制御を効かせるため、Gemini Enterprise 側で ID プロバイダーを設定済みであること        |
| **認証方式**        | **サービスアカウントでの検索は非対応**。ユーザー認証情報が必要です                            |

> ⚠️ **「スマート機能」の有効化を忘れると、どのコネクタも作成できません。** 最初に済ませてください。
> Workspace 管理コンソールの、Google Workspace のスマート機能に関する設定項目です。

> 💡 **ID プロバイダーの設定は権限デモの前提でもあります。**
> これがないと、`編集長限定` フォルダのアクセス制御が検索結果に反映されません（→ §3.2、§7.6）。

---

### 6.2 OAuth クライアントを作る（**3 コネクタ共通・1 つでよい**）

**Drive / Chat / Gmail いずれも OAuth の設定が必須**です。
ただし **1 つのクライアントを 3 つで使い回せます**。スコープをまとめて 1 回で作るのが簡単です。

> 📖 一次情報：
> [Drive](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gdrive/config) /
> [Chat](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gchat/config) /
> [Gmail](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gmail/config)

#### (1) API を有効化する

| コネクタ     | 有効化する API                                               |
| ------------ | ------------------------------------------------------------ |
| Google Drive | **Google Drive API**                                         |
| Google Chat  | **Google Chat API**                                          |
| Gmail        | **Gmail API** / **Google People API** / **Cloud Search API** |

> 💡 Google Chat API は §4.2 で既に有効化・構成済みのはずです。

#### (2) OAuth 同意画面を設定する

- ユーザーの種類：**内部（Internal）** を推奨
  （外部にする場合は**テストユーザーの追加**が必要です）
- **連絡先メールアドレス**を入力
- **Google API サービスのユーザーデータに関するポリシー**に同意

#### (3) OAuth クライアント ID を作成する

**Google Auth Platform → クライアント → クライアントを作成**

| 項目                           | 値                                                           |
| ------------------------------ | ------------------------------------------------------------ |
| **アプリケーションの種類**     | **ウェブ アプリケーション（Web application）**               |
| **承認済みのリダイレクト URI** | **`https://vertexaisearch.cloud.google.com/oauth-redirect`** |

作成後、**クライアント ID とクライアント シークレット**を控えます。

> ⚠️ **リダイレクト URI は必須です。** 未設定だと、参加者が Gemini Enterprise から
> データソースを認可しようとした時点でエラーになります。

#### (4) スコープを**同意画面に登録する**（← 忘れると `Verify Auth` が失敗します）

クライアントを作っただけでは足りません。**同意画面側にスコープを登録**します。

**Google Auth Platform → データアクセス（Data Access） → スコープを追加または削除**
→ 「**手動でスコープを追加**」欄に、次を貼り付け → **更新** → **保存**

**本ワークショップは検索のみ**なので、最小スコープで足ります。

```
https://www.googleapis.com/auth/drive.readonly
https://www.googleapis.com/auth/chat.spaces.readonly
https://www.googleapis.com/auth/chat.messages.readonly
https://www.googleapis.com/auth/gmail.readonly
```

> 💡 メール送信やファイル作成などの**アクション**を使う場合のみ、次が追加で必要です。
> 今回は不要です。
> `drive.file` / `gmail.send` / `gmail.modify` / `gmail.compose` / Chat の書き込み系スコープ

> ⚠️ `gmail.readonly` と `drive.readonly` は Google の分類で**制限付きスコープ**です。
> 同意画面が**内部（Internal）**であれば審査は不要ですが、
> Workspace 管理コンソールの **API のアクセス制御**でサードパーティ アプリをブロックしている場合、
> このクライアントを**信頼済みアプリとして許可**する必要があります（→ §6.7）。

#### ⚠️ §4.2 で作ったクライアントとは別物です

**OAuth クライアントは 2 つ登場します。** 用途が違うので混同しないでください。

|                  | **§4.2** Chat 投入スクリプト用         | **§6.2** Gemini Enterprise コネクタ用                        |
| ---------------- | -------------------------------------- | ------------------------------------------------------------ |
| 使う人           | 準備担当者のローカル Python            | **Gemini Enterprise 本体**                                   |
| 種類             | **デスクトップ アプリ**                | **ウェブ アプリケーション**                                  |
| リダイレクト URI | 不要（自動採番）                       | **`https://vertexaisearch.cloud.google.com/oauth-redirect`** |
| スコープ         | `chat.messages.create`（**書き込み**） | `*.readonly`（**読み取り**）                                 |
| 使う場面         | D-3 のデータ投入時のみ                 | 当日ずっと                                                   |

---

### 6.3 Google Drive データストア

Gemini Enterprise → **データストア** → **データストアを作成** → **Google Drive** を選択。

| 設定     | 本ワークショップでの値                          |
| -------- | ----------------------------------------------- |
| スコープ | **共有ドライブフィルタ（Shared drive filter）** |
| 対象     | 共有ドライブ **`TECHSCOPE 編集部`** の ID       |
| 暗号化   | Google 管理の鍵で可                             |

**共有ドライブ ID の調べ方**：Drive で対象の共有ドライブを開き、URL からコピーします。

```
https://drive.google.com/drive/folders/0AInoge...XXXX
                                       ~~~~~~~~~~~~~~ これが ID
```

> 💡 **「すべて（All）」ではなく共有ドライブフィルタを推奨します。**
> All にすると参加者のマイドライブまで対象になり、
> Part 2 で各自が作ったスプレッドシートが Part 3 の検索結果に混ざります。

> ⚠️ **`編集長限定` フォルダも同じ共有ドライブの中にあります。**
> これは**意図どおり**です。インデックスはされますが、権限のない参加者には返りません。
> これで §7.6 の権限デモが成立します。

> ⚠️ 公式ドキュメントの注意：データフェデレーション利用時、Drive コネクタは
> **検索するユーザーのドメインが所有する文書のみ**を対象とします。
> 本ワークショップは全データを共有ドライブに置いているため、この条件を満たします。

---

### 6.4 Google Chat データストア

**データストアを作成** → **Google Chat** を選択。

| 設定                                                 | 本ワークショップでの値 |
| ---------------------------------------------------- | ---------------------- |
| メッセージの新しさフィルタ（Message Recency Filter） | **90 日**程度          |
| ダイレクトメッセージを除外                           | **オンにする**         |

> 💡 **新しさフィルタは短すぎないように。**
> Chat API は過去日時を指定できないため、投入したメッセージは**投入した日の日付**になります（→ §4.4）。
> 直近に入るので短くても拾えますが、準備日と開催日が離れる場合に備えて余裕を持たせてください。

> 💡 **ダイレクトメッセージの除外はオンを推奨します。**
> オンにするとスペースとグループチャットのみがインデックスされます。
> 本ワークショップで必要なのは 3 つのスペースだけです。

> ⚠️ **参加者グループが 3 スペースすべてのメンバーであること。**
> メンバーでないスペースは、そのユーザーの検索結果に出ません。
> 特に **`校閲・訂正`** を落とすと、ワークショップの山場が消えます（→ §4.5）。

---

### 6.5 Gmail データストア

**データストアを作成** → **Gmail** を選択します。

`Google Mail authentication parameters` の欄に、§6.2 で控えた値を入力し、
**`Verify Auth`（認証を確認）** を押します。

| 入力欄                                      | 入れる値                                                  |
| ------------------------------------------- | --------------------------------------------------------- |
| The Client ID from your Google Mail App     | §6.2 で作った**ウェブ アプリケーション**のクライアント ID |
| The Client Secret from your Google Mail App | 同じクライアントのシークレット                            |

暗号化は Google 管理の鍵で構いません。

> ⚠️ **`Verify Auth` が通らない場合は §6.7 の切り分け表を上から順に確認してください。**
> 原因の大半は、クライアントの種類・スコープ未登録・API 未有効化のいずれかです。

---

### 6.6 同期の確認

データストアの状態が **`Creating` → `Active`** に変われば作成完了です。
そのうえで、インデックス件数を確認します。

| コネクタ     | 期待される件数の目安               |
| ------------ | ---------------------------------- |
| Google Drive | 18（共有ドライブ。編集方針を含む） |
| Google Chat  | 35 前後（メッセージ数）            |
| Gmail        | 3（参加者 1 名あたり）             |

件数が 0 のものがあれば、次を確認してください。

- **スマート機能**が有効になっているか（→ §6.1。最頻出）
- Chat：参加者グループが**スペースのメンバーになっているか**
- 共有ドライブ：参加者グループに**閲覧者**権限があるか
- Gmail：3 通が参加者の受信箱に届いているか（**1 名の実アカウントで確認**）
- `編集長限定` フォルダ：**権限を与えていないこと**（意図的）

> 💡 **必ず参加者アカウントの 1 つで実際にログインして確認してください。**
> 管理者アカウントでは見えるが参加者では見えない、という取りこぼしが最も多い失敗です。

### 6.7 つまずいたら

#### `Verify Auth` が通らないとき（上から順に確認）

| #   | 確認すること                                                    | よくある間違い                                                                                                                                                                                                              |
| --- | --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **クライアントの種類は「ウェブ アプリケーション」か**           | **§4.2 の「デスクトップ アプリ」を流用している**（最頻出）。デスクトップ アプリにはリダイレクト URI がなく、必ず失敗します                                                                                                  |
| 2   | **リダイレクト URI を登録したか**                               | `https://vertexaisearch.cloud.google.com/oauth-redirect` が未登録                                                                                                                                                           |
| 3   | **スコープを同意画面に登録したか**                              | クライアントを作っただけで、**データアクセス → スコープを追加**をやっていない（→ §6.2 (4)）                                                                                                                                 |
| 4   | **必要な API をすべて有効化したか**                             | Gmail は 3 つ必要。**Cloud Search API の有効化漏れ**が多い                                                                                                                                                                  |
| 5   | **OAuth クライアントは Gemini Enterprise と同じプロジェクトか** | 別プロジェクトで作ってしまった                                                                                                                                                                                              |
| 6   | **シークレットを正しく貼れているか**                            | 前後の空白、コピー漏れ。**一度作り直して貼り直す**のが早い                                                                                                                                                                  |
| 7   | **数分待ったか**                                                | 作成直後は反映に時間がかかることがあります                                                                                                                                                                                  |
| 8   | **Workspace の API アクセス制御でブロックされていないか**       | `gmail.readonly` `drive.readonly` は**制限付きスコープ**。管理コンソールの **セキュリティ → アクセスとデータ管理 → API の制御 → アプリのアクセス制御** で、このクライアント ID を**信頼済み**にする必要がある場合があります |

#### その他

| 症状                                   | 原因                               | 対処                                                              |
| -------------------------------------- | ---------------------------------- | ----------------------------------------------------------------- |
| データストアが作成できない             | **スマート機能が無効**             | Workspace 管理コンソールで有効化（→ §6.1）                        |
| 同じく作成できない                     | 個人アカウントでサインインしている | **Workspace と同じ組織アカウント**でサインインし直す              |
| データストアが作成できない             | OAuth クライアント未作成           | **§6.2 を実施（3 コネクタとも必須）**                             |
| 参加者が認可でエラーになる             | **リダイレクト URI 未設定**        | `https://vertexaisearch.cloud.google.com/oauth-redirect` を追加   |
| `Creating` から進まない                | 初回同期に時間がかかっている       | 時間をおいて再確認。**当日ではなく D-2 に着手する理由がこれです** |
| 検索結果に何も出ない                   | ID プロバイダー未設定              | §6.1 を確認                                                       |
| 参加者のスプレッドシートが検索に混ざる | Drive のスコープが「すべて」       | **共有ドライブフィルタ**に変更（→ §6.3）                          |
| `編集長限定` が参加者に見えてしまう    | 共有ドライブの権限設定             | 参加者グループに権限を与えていないか確認（→ §7.6）                |

---

## 7. 受け入れテスト（D-1・最重要）

### 7.1 Part 2 の確認（Sheets の Gemini）

**ここが本編の主役になったため、必ず実機で確認してください。**

| #   | 確認すること                                                            |
| --- | ----------------------------------------------------------------------- |
| 1   | Sheets のサイドパネルで Gemini が開き、`@` でドキュメントを指定できるか |
| 2   | 取材メモ 3 本から比較表が生成されるか                                   |
| 3   | 生成された表に「記載なし」が残っているか（**推測で埋めていないか**）    |
| 4   | 表からグラフが生成されるか                                              |
| 5   | 「週単位に揃えた列を追加して」が通るか                                  |

> ⚠️ **§10 のフォールバックを必ず読んでおいてください。**
> Sheets の Gemini は日本語指示で挙動が変わることがあります。
> 代替手順を持たずに当日を迎えないでください。

### 7.2 横断検索の確認

| #   | 投げるクエリ                                                 | 返ってほしいもの                                 | 出どころ              |
| --- | ------------------------------------------------------------ | ------------------------------------------------ | --------------------- |
| 1   | SOLVRA の支援実績社数は？                                    | **482社**（「約500社」ではない）                 | Drive 業績資料        |
| 2   | 楠木理生氏の役職は？                                         | **執行役員 CTO**                                 | Drive 会社概要        |
| 3   | Solvra Data Bridge の Advanced プランの提供状況は？          | **プレビュー中**                                 | Drive 広報資料        |
| 4   | 東雲デジタル銀行の導入が長引いている理由は？                 | 技術ではなく**社内の合意形成**。慎重姿勢は意図的 | Chat / Drive          |
| 5   | 京浜運輸で時間がかかったのは何の作業？                       | **指標の定義の統一**                             | Drive 取材メモ        |
| 6   | 前回の SOLVRA 取材で先方から受けた宿題は？                   | **導入期間の内訳数値**。**未回収**               | Drive 宿題・約束      |
| 7   | SOLVRA の支援実績数値を記事にする際の注意点は？              | **8月5日まで公表不可**                           | Gmail                 |
| 8   | **過去に取材先の役職を誤記した記録はある？**                 | **COR-2023-0147**                                | **Chat `校閲・訂正`** |
| 9   | TabFM の BigQuery 統合について、日本の現場を踏まえた論点は？ | **SQL は書けるが Python は書けない情シスが多い** | Drive ヒアリングメモ  |

> ⚠️ **#8 が返らなければ、Part 3 の山場が成立しません。**
> Chat スペースのメンバー設定とコネクタの同期対象を確認してください。

### 7.3 統合クエリ

```
明日 Google Cloud のデータ分析部門 VP に取材します。
事前に SOLVRA 社にヒアリングした内容、導入企業3社の取材メモ、
過去記事、同僚が残したメモ、広報とのやり取り、過去の取材記録を調べて、
確認すべき論点を出典付きで整理してください。
導入企業3社の共通点と、導入期間に差がついた要因も併せてお願いします。
```

**期待される要素**：3 社とも試用開始は 1 週間以内／差の要因は導入側の準備／業績資料が裏付け／宿題が未回収／公表制約あり

> ⚠️ **出力に Google Cloud 側の数値や発言が混ざっていないか確認してください。**
> データに一切入れていないため、出てきた場合は AI の推測です。
> **これは当日むしろ格好の教材になります**（→ facilitator-guide §2）。

### 7.4 原稿レビューエージェントの確認

HANDSON.md の手順どおり、**編集方針を冒頭に付けて** `draft-v1` を貼り付けます。

**事実の誤り 10 項目**（story-bible §6.2）が指摘されることを確認してください。
特に次は必ず出ることを確認します。

- **「取締役 CTO」→ 執行役員 CTO（＋ COR-2023-0147 への言及）** ← **最大の山場**

### 7.5 編集方針レビューの確認

**事実としては正しいが方針に反する 3 か所**（story-bible §6.4）が、
**【方針】として分類されて指摘される**ことを確認してください。

| 原稿の記述                                              | 期待される分類           |
| ------------------------------------------------------- | ------------------------ |
| 「BigQuery は、いまや事実上の標準になったと言っていい」 | 【方針】炎上リスク       |
| 「さらに時間がかかっているのが東雲デジタル銀行である」  | 【方針】トーン（序列化） |
| 「そうした未来は、もはや遠い話ではない」                | 【方針】主題からの逸脱   |

さらに、**読者ペルソナを経営層に変えて再実行**し、**指摘の優先順位が変わる**ことを確認します。

> ⚠️ 方針違反が【事実】側に混ざって出る、または優先順位が変わらない場合は、
> エージェントの指示から「■ 記者が伝える編集方針を最優先する」の節が欠けていないか確認してください。

### 7.6 権限の継承（権限デモを行う場合）

**参加者アカウントでログインした状態**で、次を検索してください。

| 投げるクエリ                         | 期待される結果                       |
| ------------------------------------ | ------------------------------------ |
| `クロシオ`                           | **0 件**                             |
| `PROJECT-KUROSHIO について教えて`    | **見つからない旨の回答**             |
| `編集長限定の資料には何がありますか` | **フォルダの存在も内容も出てこない** |
| `要員計画`                           | **0 件**                             |

> ⚠️ **1 件でもヒットしたら、`編集長限定` フォルダの権限設定が誤っています。**
> 参加者グループに権限が付いていないか確認してください。
>
> 逆に、**管理者アカウントでは正常にヒットする**ことも確認しておくと、
> 「インデックスされているが権限で見えない」状態であることを当日説明できます。

---

## 8. 参加者アカウントの準備（D-1）

- [ ] 参加者 20 名分のアカウントを作成し、Gemini Enterprise へのアクセスを付与
- [ ] **Google グループ `techscope-workshop@` を作成し、20 名を追加**
- [ ] 共有ドライブ `TECHSCOPE 編集部` に**グループを閲覧者として**追加
- [ ] **Chat の 3 スペースにグループを追加**（特に `校閲・訂正`）
- [ ] **Gmail 3 通を広報役アカウントからグループ宛に送信**
- [ ] `編集長限定` フォルダには**権限を与えない**
- [ ] **参加者番号 01〜20 のカードを印刷し、席に配置**
- [ ] Agent Designer が参加者アカウントで使えることを 1 名で確認
- [ ] **参加者アカウント 1 つで実際にログインし、§7 の受け入れテストを再走**

> ⚠️ 最後の項目が最重要です。管理者では見えるが参加者では見えない、が最も多い失敗です。

---

## 9. リハーサル（D-1）

| 確認項目   |                                                                            |
| ---------- | -------------------------------------------------------------------------- |
| Part 1     | ブログ URL が読めるか。読めない場合の代替が機能するか。Canvas で図が出るか |
| **Part 2** | **§7.1 の 5 項目すべて。ここが本編の主役**                                 |
| Part 3     | §7.2 の全クエリ、特に **#8（COR-2023-0147）**                              |
| Part 3     | Agent Designer で新規作成 → 保存 → 実行まで通るか                          |
| 全体       | 各パートが 20 分に収まるか                                                 |

所要時間を実測し、[`docs/facilitator-guide.md`](docs/facilitator-guide.md) の時間配分を更新してください。

---

## 10. 当日のバックアップ

| 起きうること                            | 対処                                                                                                                                                                     |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Sheets の Gemini が表を作らない**     | **Docs の Gemini で表を作らせ、手でコピー&ペースト。**Part 2 の後半（グラフ・単位揃え）は成立します                                                                      |
| **Sheets の Gemini がグラフを作らない** | 表を選択して「挿入 → グラフ」で手動作成。Gemini には「**この表から何が読み取れるか**」を聞く形に切り替え                                                                 |
| **単位を揃える指示が通らない**          | 換算列を講師が手で 1 行入れて見せ、残りを AI に埋めさせる                                                                                                                |
| Chat の訂正記録が出ない                 | スペースのメンバー設定と同期を確認。ダメなら講師画面で Chat を直接開いて見せる（**「ここにあったのに誰も見ていなかった」というメッセージは、手動で見せても成立します**） |
| エージェント作成が通らない              | 講師作成の「編集部標準・原稿レビューエージェント」を全員が使う形に切り替え                                                                                               |

> **リハーサル時に画面録画を残しておいてください。** 全面的に詰まった場合の最後の保険になります。

---

## 11. 参考リンク

- [Agent Designer でエージェントを作成する](https://docs.cloud.google.com/gemini/enterprise/docs/agent-designer/create-agent)
- コネクタの OAuth 設定
  - [Google Drive](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gdrive/config)
  - [Google Chat](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gchat/config)
  - [Gmail](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gmail/config)
- コネクタのデータストア作成
  - [Google Drive](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gdrive/set-up-data-store)
  - [Google Chat](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gchat/set-up-data-store)
  - [Gmail](https://docs.cloud.google.com/gemini/enterprise/docs/connectors/gmail/set-up-data-store)
- [Google Chat API を構成する](https://developers.google.com/workspace/chat/configure-chat-api)
- [Part 1 で読む記事：Introducing TabFM](https://research.google/blog/introducing-tabfm-a-zero-shot-foundation-model-for-tabular-data/)
